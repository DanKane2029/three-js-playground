import { ShaderMaterialParameters } from "three";

/**
 * A function that creates a {{sentenceCase name}} shader programs and uniforms.
 *
 * @returns The shader parameters.
 */
const {{pascalCase name}} = (): ShaderMaterialParameters => {
	return {
		uniforms: { },

		vertexShader: /*glsl*/ `
		varying vec2 xy;

		void main() {
			vec4 modelViewPosition = modelViewMatrix * vec4(position, 1.0);
			gl_Position = projectionMatrix * modelViewPosition;
			xy = gl_Position.xy;
		}`,

		fragmentShader: /*glsl*/ `
		varying vec2 xy;

		void main() {			
			gl_FragColor = vec4(1.0, 0.0, 1.0, 1.0);
		}`,
	};
};

export { {{pascalCase name}} };
